http:
  middlewares:
    https_redirect:
      redirectScheme:
        scheme: https
        permanent: true
tls:
  stores:
    default:
      defaultCertificate:
        certFile: /certs/wildcard.docker.test.crt
        keyFile: /certs/wildcard.docker.test.key
